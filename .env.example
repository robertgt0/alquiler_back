MONGO_URI=mongodb+srv://<usuario>:<contraseña>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# Copia este archivo a `.env` y reemplaza los valores entre <> con tus credenciales reales.
# No subas tu archivo `.env` a repositorios públicos.
